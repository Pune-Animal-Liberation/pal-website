---
const currentPath = Astro.url.pathname;

const links = [
  { href: '/', label: 'Home' },
  { href: '/about/', label: 'About' },
  { href: '/blog/', label: 'Blog' },
  { href: '/events/', label: 'Events' },
  { href: '/resources/', label: 'Resources' },
  { href: '/contact/', label: 'Contact' },
];
---

<nav class="nav" aria-label="Main navigation">
  <div class="container">
    <div class="nav__inner">
      <a href="/" class="nav__brand" aria-label="Pune Animal Liberation — Home">
        <img src="/favicon-optimized.png" alt="Pune Animal Liberation logo" class="nav__logo" width="40" height="40" loading="lazy" decoding="async" />
      </a>
      <ul class="nav__links" role="list">
        {links.map((link) => (
          <li>
            <a
              href={link.href}
              aria-current={
                link.href === '/'
                  ? currentPath === '/'
                    ? 'page'
                    : undefined
                  : currentPath.startsWith(link.href)
                    ? 'page'
                    : undefined
              }
            >
              {link.label}
            </a>
          </li>
        ))}
        <li>
          <a href="/get-involved/" class="nav__cta">Contact Us</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
